import { styled } from "nativewind";
import { Pressable as StyledPressable, Text as StyledText } from "react-native";

const Pressable = styled(StyledPressable);
const Text = styled(StyledText);

export { Pressable, Text };
