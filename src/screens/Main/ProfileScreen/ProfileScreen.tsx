import { Profile } from "@/components/Profile";
import { Container } from "@/shared/ui";

const ProfileScreen = () => {
  return (
    <Container>
      <Profile />
    </Container>
  );
};

export default ProfileScreen;
