import { PostList } from "@/components/PostsList";
import { Container } from "@/shared/ui";

const PostsScreen = () => {
  return (
    <Container>
      <PostList />
    </Container>
  );
};

export default PostsScreen;
